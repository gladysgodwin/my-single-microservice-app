apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-helm-charts.fullname" . }}
  labels:
    {{- include "prometheus-helm-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-helm-charts.selectorLabels" . | nindent 4 }}
