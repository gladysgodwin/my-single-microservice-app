apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-helm-charts.fullname" . }}
  labels:
    {{- include "grafana-helm-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-helm-charts.selectorLabels" . | nindent 4 }}
